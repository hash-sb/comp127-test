name: Deploy Superfluous Site to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: false 

      - name: Install Dependencies
        run: |
          bundle config set --local frozen false
          bundle config set --local with 'development'
          bundle install

      - name: Build site
        # Removed the invalid --site-url flag
        run: bundle exec superfluous .

      - name: Fix Paths for GitHub Pages
        # This shell script finds all HTML and CSS files and replaces 
        # absolute paths (starting with /) with relative paths or 
        # adds the repository name prefix.
        run: |
          find output -name "*.html" -type f -exec sed -i 's|href="/|href="/comp127-test/|g' {} +
          find output -name "*.html" -type f -exec sed -i 's|src="/|src="/comp127-test/|g' {} +

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./output

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4